export enum MenuType {
  Group,
  Collapse,
  Item,
}

export enum PhaseWorkType {
  Prepare,
  Invest,
  Finish,
}

// action type datagrid
export enum ActionDataGrid {
  Create,
  Save,
  Delete,
}
